

const SendRequestButton = () => {
  return (
    <button
      className="max-w-[310px] w-full h-[50px] mt-[35px] bg-orange hover:bg-black transition-all">
      ОТПРАВИТЬ
    </button>
  );
};

export default SendRequestButton;
